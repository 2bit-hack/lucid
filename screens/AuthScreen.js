import React from 'react';
import {
  StyleSheet,
  Text,
  View,
  ScrollView,
  KeyboardAvoidingView,
} from 'react-native';

const AuthScreen = () => {
  return (
    <View>
      <Text />
    </View>
  );
};

const styles = StyleSheet.create({});

export default AuthScreen;
